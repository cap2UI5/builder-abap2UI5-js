name: samples transpile

# Transpile all sample classes from run/input/samples/src into run/output/samples
# (+ transpile report). Step 2 of the update_samples pipeline.

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.ref_name }}
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci
      - run: npm run transpile_samples
      # A `with:` input is never run through a shell — a $(…) in the commit
      # message would land in the history verbatim. Resolve it here instead.
      - name: read upstream sha
        id: upstream
        run: echo "short=$(cut -c1-12 run/input/samples/UPSTREAM_COMMIT)" >> "$GITHUB_OUTPUT"
      - name: commit
        uses: ./.github/actions/commit-generated
        with:
          paths: run/output/samples
          message: "transpile: update run/output/samples (${{ steps.upstream.outputs.short }})"
